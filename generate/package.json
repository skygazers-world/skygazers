{
  "name": "generate",
  "version": "1.0.0",
  "description": "``` mkdir -p out node mkindex.js ```",
  "main": "mkindex.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^3.2.4",
    "ipfs-only-hash": "^4.0.0"
  }
}
